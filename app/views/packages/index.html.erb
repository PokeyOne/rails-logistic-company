<h1>Pokey Transit and Logistics Corporation</h1>
<span class="disclaimer"><i>This is a fake logistics company system created for the Shopify job application process.</i></span>

<p>
  Welcome to the website of the Pokey Transit and Logistics Corporation. This is the inventory management system for the company.
  Packages are recorded here and can be viewed and edited.
</p>

<h2>Packages</h1>

<div class="button button-blue">
  <%= link_to "Create New Package", new_package_path %>
</div>

<table class="package-list">
  <tr class="package-list-header package-list-row">
    <th>ID</th>
    <th>Name</th>
    <th>From Address</th>
    <th>To Address</th>
    <th>State</th>
    <th>Edit</th>
    <th>Delete</th>
  </tr>
  <% @packages.each do |package| %>
    <tr class="package-list-row">
      <td class="package-list-data"><%= package.id %></td>
      <td class="package-list-data"><%= package.name %></td>
      <td class="package-list-data"><%= package.from_address.one_liner %></td>
      <td class="package-list-data"><%= package.to_address.one_liner %></td>
      <td class="package-list-data"><%= package.state %></td>
      <td class="package-list-data">
        <%= link_to "Edit", edit_package_path(package) %>
      </td>
      <td class="package-list-data">
        <%= link_to "Destroy", package_path(package), data: {
          turbo_method: "delete",
          turbo_confirm: "Are you sure you want to delete this package?"
        } %>
      </td>
    </tr>
  <% end %>
</table>
